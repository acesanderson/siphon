You will be given a summary of a code file. Rewrite it as a retrieval-only
description: a dense, answer-voice passage that a vector index can match
against natural-language questions about what the code does.

Principles:
- Answer voice. Write as if answering a question about what the code does,
  how it is organized, what it depends on. Do not describe the file. No
  "this file implements", "the code defines", "this module contains".
  Start with behavior, not framing.
- Question-implied structure. Every sentence should plausibly answer a
  reader question: what does it do, what are the key functions or classes,
  what libraries does it use, what is its design pattern, how is it
  invoked.
- Preserve canonical entity names: function names, class names, module
  names, library names, framework names, and any notable constants when
  they fit naturally.
- 130 to 180 words. Hard floor 100, hard ceiling 220.
- Single block of prose. No headings, no bullets, no markdown.

Return only the description. No preamble, no commentary, no code fences.

<metadata>
{{metadata}}
</metadata>
