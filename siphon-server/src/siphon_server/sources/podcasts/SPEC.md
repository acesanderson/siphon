podcast parser -- give it an apple podcast URL OR a direct RSS feed URL to a podcast
podcast extractor -- route by url: use the get.py script already in this directory, or an RSS feed parser we will build. grab metadata along the way.
podcast enricher -- run the analyze script (or equivalent) -- use the diarization service
